# 本地画廊数据、缓存与备份重构计划

## 1. 目标与最终行为

将当前分散在 `GallerySummary`、`GalleryDetail`、SwiftData、下载任务、详情 JSON 缓存和图片缓存中的画廊信息收敛为统一的本地数据层，并让首页、浏览/搜索、收藏、历史、下载列表、详情页和阅读器使用同一套解析结果。

最终行为如下：

- 已下载画廊始终优先使用本地保存的稳定信息、动态快照和媒体文件。
- 普通缓存画廊始终优先使用本地稳定信息和已加载图片，但评分、收藏和评论等动态信息不进入普通缓存。
- 普通缓存详情打开时先立即展示稳定信息，再实时请求动态信息；请求失败不影响稳定内容展示。
- 已下载详情打开时不自动联网，动态信息使用下载时或上次手动刷新时保存的快照。
- 详情页右上角提供直接可见的刷新按钮，用于刷新当前画廊。
- 设置中的“更新已下载画廊信息”同时负责：
  - 通过轻量批量接口刷新已下载画廊可获得的摘要信息；
  - 补全批量接口能够提供的稳定字段，详情专属字段保留为待补全；
  - 独立处理每个批次，单个批次无法访问时继续更新其他项目。
- 不缓存首页、搜索等列表的查询条件、顺序和分页快照，只缓存列表里出现过的画廊条目及成功加载的封面。
- 移除“启用详情缓存”开关，缓存成为固定行为；设置中保留占用统计和手动清理。
- 所有逻辑兼容 iOS、iPadOS 和 macOS，共享领域模型、持久化、缓存和归档实现。

## 2. 数据分类与字段定义

### 2.1 稳定画廊信息

普通缓存、下载记录、`.ehgallery` 和 `.eharchive` 都使用同一份稳定信息模型。至少包含：

- 画廊标识：`gid`、`token`；
- 最近一次成功获取信息时使用的站点；
- 普通标题；
- 日文标题；
- 作者数组：只取全部 `artist:` 标签，一个画廊允许多个作者；
- 上传者 `uploader`：与作者分开保存和展示；
- 全部原始标签，保留命名空间；
- 分类 `category`，即 `DOUJINSHI`、`IMAGE SET` 等彩色分类；
- 可选的详情原始语言，例如 `English`、`Chinese`；
- 页数；
- 上传时间；
- 封面地址；
- 文件大小；
- 简介；
- 可分享的外部详情地址；
- 已获取的全部预览页描述：页码、页面地址、预览地址和裁切参数。

语言沿用现有机制：

- 详情页原始 `Language:` 文本作为可选字段持久化；
- `ZH`、`EN` 等短码继续从 `language:` 标签派生，并保留现有标题规则作为后备；
- 没有语言信息的画廊保持未知，不生成默认语言；
- 短码是确定性派生值，不再额外保存一份可能与标签冲突的数据。

作者字段由 `artist:` 标签规范化得到，同时保留完整原始标签。导入旧备份时，如果没有独立作者数组，则从标签重新生成。

### 2.2 下载专属动态快照

以下动态信息不进入普通缓存，只有加入下载列表后才允许持久化，并随下载备份迁移：

- 评分；
- 评分人数；
- 收藏次数；
- 收藏名称；
- 收藏分类；
- 评论作者；
- 评论正文；
- 评论发布时间；
- 评论分数；
- 动态快照最后成功刷新时间。

不保存以下会话相关或短期有效数据：

- API UID、API Key；
- 评论编辑权限、投票权限和当前账户投票状态；
- torrent 临时地址；
- 站点 archive 临时地址；
- 其他依赖当前登录会话的操作令牌。

### 2.3 本地使用状态

以下信息继续作为本地应用状态独立保存，不属于画廊内容快照，也不进入 `.ehgallery` 或 `.eharchive`：

- 阅读进度和最后阅读时间；
- 本地收藏状态；
- 下载标签；
- 下载任务状态和重试状态；
- 搜索历史；
- 过滤规则；
- 阅读设置和应用设置；
- Cookie、密码或其他登录凭据。

分离内容快照和本地使用状态，确保清缓存、导入备份和元数据更新不会覆盖阅读进度或下载状态。

## 3. 领域模型与公共接口

### 3.1 新的统一快照类型

在 `EHDomain` 中定义可 `Codable`、`Hashable`、`Sendable` 的版本化传输类型：

- `StableGalleryMetadataSnapshot`
  - 包含全部稳定信息；
  - 包含稳定信息捕获时间；
  - 包含字段完整度。
- `DownloadedGalleryDynamicSnapshot`
  - 包含评分、收藏和评论快照；
  - 包含动态信息捕获时间。
- `GalleryTransferRecord`
  - 包含格式版本；
  - 包含稳定快照；
  - 可选包含下载动态快照；
  - 包含导出时间和来源站点；
  - 不直接包含图片字节。

逻辑画廊身份继续使用 `GalleryKey(gid, token)`，避免同一画廊在 E-Hentai 和 ExHentai 下产生两套本地记录。快照记录最近来源站点，图片缓存键则包含站点和完整 URL，避免不同站点资源互相覆盖。

### 3.2 字段完整度

扩展现有 `GalleryMetadataCompleteness`，至少覆盖：

- 普通标题；
- 日文标题；
- 作者；
- 上传者；
- 标签；
- 分类；
- 语言；
- 页数；
- 上传时间；
- 封面；
- 文件大小；
- 简介；
- 预览描述；
- 评分；
- 评分人数；
- 收藏信息；
- 评论。

每个字段必须能够区分：

- 已成功加载且有值；
- 已成功加载但站点确认没有值；
- 尚未加载或加载失败。

旧格式只包含标题、日文标题和标签完整度时，其余字段默认标记为尚未加载，而不是错误地标记为空值。

### 3.3 合并规则

所有缓存、下载、导入和刷新统一使用字段级合并器，不允许各页面自行拼接字段：

1. 完整字段优先于不完整字段。
2. 新值缺失或尚未加载时，不覆盖已有完整值。
3. 双方字段都完整时，使用所属快照捕获时间较新的值。
4. 导入只更新内容快照，不覆盖阅读进度、本地收藏、下载标签和任务状态。
5. 普通缓存不能写入动态快照。
6. 网络列表返回的实时评分等动态值只能用于当前界面，不写入普通缓存。
7. 下载画廊的动态快照只有加入下载、刷新当前详情或设置批量更新时才能写入。

## 4. SwiftData 与现有数据迁移

### 4.1 Schema 升级

新增下一版 `VersionedSchema`，并在迁移计划中保留现有 V1、V2 到新版的迁移链。新版持久化结构需要明确区分：

- 画廊身份与本地使用状态；
- 稳定内容快照；
- 下载动态快照；
- 预览页描述；
- 缓存保留原因和下载保留原因；
- 稳定信息与动态信息的捕获时间；
- 字段完整度。

关系必须声明明确 inverse 和删除规则。画廊内容记录被移除时，其预览描述级联移除；本地状态记录不能因为清除普通缓存而被误删。

### 4.2 现有记录迁移

- 将 `GalleryRecord` 现有的标题、日文标题、封面地址、分类、页数、上传时间、上传者和标签迁入稳定快照。
- 从现有标签生成作者数组和语言短码派生所需数据。
- 现有 `rating`、`ratingCount`、`favoriteCategory` 只有在画廊存在下载任务时迁入下载动态快照；否则不再作为普通缓存保存。
- 现有记录没有捕获时间时使用最低优先级的历史时间标记，使后续明确刷新或备份导入可以覆盖它们。
- 保留原有阅读进度、最后阅读时间和本地收藏状态。
- 下载任务现有标题、日文标题、标签和页面描述与统一稳定快照合并，下载任务不再作为画廊信息的独立真相来源。

### 4.3 现有详情和图片缓存迁移

- 启动时识别旧 `GalleryCacheStore/Details` JSON。
- 将其中的稳定字段和预览描述按字段级合并规则迁入 SwiftData。
- 丢弃普通缓存 JSON 中可能存在的评分、收藏和评论等动态字段。
- 旧图片缓存作为只读后备来源；图片首次命中时登记到新的画廊图片索引，避免一次性复制大量文件。
- 只有在某个旧详情记录成功迁移后才记录其迁移完成状态；迁移中断后可以安全继续。

## 5. 缓存实现

### 5.1 普通元数据缓存

- 列表中出现画廊时保存当前稳定摘要。
- 列表封面实际加载成功后登记为该画廊的本地封面。
- 打开详情并成功解析后保存稳定详情和预览描述。
- 普通缓存永远不保存评分、收藏和评论。
- 不保存列表查询、条目顺序、分页游标或搜索结果快照。

### 5.2 封面和预览图片

建立可枚举的图片索引，至少记录：

- 画廊标识；
- 来源站点；
- 图片类型：封面或预览；
- 预览页码；
- 原始 URL；
- 本地文件位置；
- 字节数；
- 最后访问时间。

只缓存界面实际成功加载的封面和预览图，不为了建立缓存主动下载未展示图片。持久磁盘缓存不按容量或时间自动淘汰，只有用户执行清理时移除；内存解码缓存仍可自行释放。

### 5.3 清除缓存

设置中的“清除缓存”执行以下操作：

- 清除全部普通缓存稳定快照；
- 清除全部普通缓存封面和预览图片；
- 清除旧详情 JSON 和旧图片缓存的可迁移索引；
- 不清除下载媒体文件；
- 不清除下载画廊稳定快照和动态快照；
- 不清除阅读进度、本地收藏和下载状态。

如果某个画廊同时属于普通缓存和下载列表，只移除普通缓存图片，下载所需内容快照必须保留。

## 6. 全局本地优先读取

在 `AppModel` 上方建立统一的本地画廊仓库接口，视图和页面模型不再分别查询 SwiftData、下载任务和详情缓存。核心能力包括：

- 批量用本地稳定快照覆盖网络列表摘要；
- 读取单个画廊的本地稳定快照；
- 读取下载画廊的动态快照；
- 保存普通缓存稳定信息；
- 将普通缓存提升为下载快照；
- 保存当前详情刷新结果；
- 查询需要补全的下载字段；
- 清除普通缓存但保留下载和本地状态。

各位置行为如下：

- 首页、浏览和搜索：网络负责返回列表及当前动态值，本地稳定字段覆盖同一画廊的标题、作者、标签、分类、语言、页数、上传时间和封面。
- 历史和收藏：从本地状态取得条目，再由统一仓库补充稳定信息。
- 下载列表：完全使用下载稳定快照和下载媒体生成封面，不依赖网络。
- 普通缓存详情：立即显示稳定缓存；评分、收藏和评论区域发起实时请求，失败时显示不可用状态，不使用旧动态缓存。
- 下载详情：显示本地稳定信息、动态快照和媒体，不自动联网。
- 阅读器：优先使用下载媒体，其次使用当前已缓存的页面描述；不得为了显示标题或页数额外请求详情。

## 7. 下载与手动刷新

### 7.1 加入下载

加入下载时：

- 将当前稳定缓存提升为下载稳定快照；
- 保存当时已经取得的评分、收藏和评论动态快照；
- 保存完整预览描述和页面描述；
- 立即持久化已取得字段及其完整度；
- 缺失字段不阻止媒体下载；
- 后续通过当前详情刷新或设置批量更新继续补全。

### 7.2 当前详情刷新

详情页右上角提供直接可见的刷新按钮，不藏在更多菜单中。刷新行为：

- 普通缓存画廊：刷新稳定信息，动态信息只用于当前界面，不持久化；
- 下载画廊：刷新稳定信息、评分、收藏和评论，并写回下载快照；
- 刷新失败时继续显示现有本地数据；
- 成功字段立即合并，缺失或失败字段保留原值和不完整状态；
- “在站点打开”等次要操作保留在更多菜单。

### 7.3 设置批量更新与缺失字段补全

“更新已下载画廊信息”按钮同时承担刷新和修复职责：

1. 读取所有下载项的字段完整度。
2. 使用每次最多 25 个画廊的 `gdata` 批量请求，更新该接口能够提供的标题、日文标题、作者标签、上传者、分类、页数、上传时间、缩略图和平均评分等信息。
3. 批量更新不请求完整详情、不加载预览分页，也不刷新评论、收藏分类、文件大小、语言、简介或完整预览描述；这些字段只在用户打开详情、手动刷新详情或开始下载时按需获取。
4. 对已经存在的下载动态快照，只保留批量接口未提供的评分人数、收藏和评论等字段，不用空值覆盖本地数据。
5. 每个批次成功后立即保存，不等待额外详情请求。
6. 某个批次无法连接、已删除、无权限、限流或解析失败时：
   - 保留现有数据；
   - 保留缺失字段状态；
   - 记录失败原因；
   - 继续处理后续画廊。
7. 只有用户主动取消或本地持久化发生无法继续的致命错误时，才终止整个任务。

批量更新保持顺序批处理，避免同时打开过多 API 请求；必须支持任务取消和逐批进度。最终结果分为：

- 完全更新；
- 部分补全；
- 无法连接；
- 仍有缺失字段；
- 未发生变化。

结果摘要显示各类数量，并允许查看失败画廊及原因。再次执行时根据完整度继续补全，不重复丢弃之前成功取得的数据。

## 8. `.ehgallery` 格式

### 8.1 导出范围与内容

默认只导出下载列表中的画廊，包括尚未完成或暂停的下载任务。每个条目包含：

- `GalleryTransferRecord`；
- 稳定信息；
- 下载动态快照；
- 评论展示快照；
- 预览页描述；
- 字段完整度和捕获时间。

`.ehgallery` 不包含：

- 封面图片字节；
- 预览图片字节；
- 漫画媒体字节；
- 本地阅读、收藏、标签和任务状态；
- 登录或会话信息。

导出允许字段不完整。包内必须保留每个字段的完整度，导出完成结果需要提示不完整画廊数量，但不能因为单个字段缺失阻止整个导出。

### 8.2 导入行为

- 验证 UTType、扩展名、归档结构、版本、条目数量和负载大小。
- 读取旧 v1 `GallerySyncSnapshot` 和新版本记录。
- 旧包缺少的字段标记为尚未加载。
- 使用字段级择优规则与本地内容合并。
- 为本地不存在的画廊创建暂停的下载任务。
- 恢复稳定信息和下载动态快照，但不恢复任何图片字节。
- 单个条目无效时记录并继续导入其他合法条目；只有归档整体不可读或安全校验失败时才终止。

## 9. `.eharchive` 格式

### 9.1 导出内容

保持现有全部漫画媒体和页面令牌结构，并为每个画廊增加：

- 与 `.ehgallery` 相同的 `GalleryTransferRecord`；
- 当前本地已有的封面图片；
- 当前本地实际加载过的预览图片；
- 图片清单，包括类型、页码、文件名、大小和对应 URL。

导出不得为了补齐封面或预览而联网。没有加载过的预览不写入包中。现有漫画媒体导出流程和校验继续保留。

### 9.2 导入行为

- 支持现有 `.eharchive`、旧 `.zip`、历史 `.ehviewer` 元数据和新版本清单。
- 在读取任何文件前验证安全路径、条目大小、媒体类型和总空间需求。
- 先解析并验证画廊元数据，再恢复图片和漫画媒体。
- 使用字段级规则合并本地稳定信息和动态快照。
- 将封面和预览恢复到新的画廊图片缓存并重建索引。
- 将漫画媒体恢复到下载文件存储并合并下载任务。
- 单个画廊或单个媒体条目失败时尽可能继续恢复其他内容，并在结果中列出失败项。

### 9.3 兼容方向

- 新版本 App 必须能够读取当前 `.ehgallery` v1 和现有/历史 `.eharchive`。
- 新包使用明确的新格式版本。
- 不要求旧版本 App 理解新增字段或读取新格式。
- 新解析器遇到未来未知版本时明确报“不支持的版本”，不能把它当作损坏旧包处理。

## 10. 设置页与迁移任务协调

将数据迁移、更新和备份操作统一交给一个任务协调器，避免当前多个布尔状态和临时文件流程互相竞争。协调器负责：

- 保证同一时间只运行一个导入、导出或批量更新任务；
- 统一任务阶段、进度、当前画廊、成功数和失败数；
- 支持取消；
- 保留逐项成功结果；
- 管理安全作用域文件访问；
- 管理导入暂存文件和导出临时文件；
- 在分享、保存、取消或失败后正确结束文件生命周期；
- 生成统一的结果摘要。

设置页调整为：

- 移除“启用详情缓存”；
- 保留并更新“缓存占用”；
- 保留“清除缓存”，文案明确不会删除下载媒体或本地状态；
- “更新已下载画廊信息”显示缺失字段补全和动态刷新进度；
- `.ehgallery` 和 `.eharchive` 导入导出显示一致的阶段进度与结果；
- 结果中区分完全成功、部分成功、跳过、无法连接、无权限、格式无效和字段仍不完整。

## 11. 跨平台实现约束

- 领域模型、SwiftData、缓存索引、格式解析、导入导出和任务协调器全部放在共享模块中。
- iOS 和 iPadOS 使用 `fileImporter` 与系统分享面板。
- macOS 使用 `fileImporter` 和保存面板，避免一次性把大型归档读入内存。
- 三个平台使用相同 UTType、扩展名识别、版本校验和错误分类。
- 所有大型归档和媒体操作使用流式读写。
- SwiftData `ModelContext` 和模型实例不得跨 actor；跨并发边界只传递值类型快照、画廊键或持久标识。
- 批量补全使用结构化并发、有限并发和协作取消；单项网络失败作为结果值返回，不抛出并取消整个任务组。
- 不引入第三方持久化、缓存或归档框架。

## 12. 实施顺序

1. 在 `EHDomain` 中建立稳定快照、下载动态快照、完整度和传输记录。
2. 升级 SwiftData Schema，并实现旧记录、详情 JSON 和图片缓存的兼容迁移。
3. 建立统一的本地画廊仓库和字段级合并器。
4. 改造列表、资料库、下载、详情和阅读器，全部通过仓库解析本地优先数据。
5. 改造下载入队、当前详情刷新和设置批量补全流程。
6. 升级 `.ehgallery` 读写格式并保留 v1 导入。
7. 升级 `.eharchive` 元数据与预览清单，并保留旧归档导入。
8. 统一设置页任务协调、进度、临时文件和三平台文件交互。
9. 最后移除旧详情缓存读取分支和重复的页面级元数据拼接逻辑；只有在新迁移路径确认成功后才停止使用旧格式。

## 13. 明确假设与非目标

- “作者”只指 `artist:` 标签，不把上传者当作作者。
- `DOUJINSHI`、`IMAGE SET` 等字段统一称为“分类”。
- 普通缓存不保存任何动态信息。
- 下载动态快照只有手动刷新才更新，不自动后台刷新。
- 普通缓存需要更新稳定信息时，由用户清除缓存后重新访问；设置批量更新只处理下载列表。
- `.ehgallery` 默认只包含下载列表，不导出所有普通缓存或历史记录。
- `.ehgallery` 不包含图片；`.eharchive` 包含漫画媒体以及本地已有封面和预览。
- 备份允许字段不完整，不因为单个缺失字段阻止下载或导出。
- 本计划不包含测试计划。
- 实施时必须保留当前工作区中已有的浏览、详情、下载、资料库和相关测试修改，不覆盖或回滚无关未提交内容。
