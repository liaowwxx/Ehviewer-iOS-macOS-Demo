# 本地画廊性能、启动与元数据补全优化

## 结论

批量更新没有把部分数据写入本地，并非单纯的列表显示问题。

已确认的原因包括：

- 批量更新只调用 gdata；任一批次失败后直接跳过，没有重试。
- gdata 中单本画廊的 `error` 或缺失响应会被解析器静默丢弃。
- 更新完成提示只有“更新 x/y”，无法区分失败与字段仍不完整。
- 当前完整度判断未包含 rating。
- 字段缺失与字段明确为空没有被可靠区分，可能错误标记为已加载。
- 本地详情优先使用 `DownloadJob` 中的旧标题和 tags，可能覆盖 SwiftData 中较新的数据。
- 已打开的本地列表会永久缓存旧摘要，数据库更新后不会重新读取。

因此需要同时修复批量获取、字段合并、本地数据源优先级和 UI 缓存失效。

## 本地列表与滚动优化

- 移除标题动态高度测量、元数据骨架动画、`Array(jobs.enumerated())` 和基于可见索引反复重建的预取任务。
- 元数据更新、下载完成或删除时发布本地库 change/revision，使已打开的本地页立即替换对应数据。
- 本地详情以持久化的稳定/动态元数据为第一数据源；`DownloadJob` 只提供页面描述和缺失字段的兜底值。

## 封面加载

- 不再为每个列表行反复读取和解析原始第一页大图。
- 增加可再生成的磁盘缩略图缓存，首次显示时按需生成，之后跨启动复用。
- 统一封面加载器负责请求去重、内存缓存、取消和并发限制；ImageIO 解码最多同时运行 2 个。
- 修正 `NSCache` cost，按实际像素字节计算。
- 列表封面更新不执行淡入动画；网格模式可保留轻量动画。
- 现有漫画的缩略图不在启动时批量生成。


## “更新已下载画廊信息”重构

### 更新模式

点击设置项后提供：

- “更新全部”：处理全部本地画廊。
- “仅更新缺失信息”：只处理存在 `.notLoaded` 列表字段的画廊。

缺失判断必须包含标题、作者、分类、页数、上传日期、缩略图和评分；rating 纳入摘要完整度。

### 第一阶段：gdata 批量更新

- 每批最多 25 本。
- 解析结果保留每个 key 的成功、单项 API 错误和响应缺失，不再静默 `compactMap` 丢弃。
- 字段状态严格区分：
  - 返回有效值：`.loadedWithValue`；
  - 接口明确返回空值：`.loadedEmpty`；
  - 字段不存在、解析失败或单项错误：`.notLoaded`。
- 不再把缺失标题替换为 `Gallery <gid>` 并标记完整。
- 首轮结束后，仅对失败或仍不完整的 key 再执行最多两轮 gdata 重试，退避 1 秒、2 秒。
- 认证失败、明确限流或流量限制时停止继续轰炸接口，并把剩余项目计入失败。

### 第二阶段：单本详情补全

- 两轮 gdata 重试后，对仍缺彩色分类、评分、上传日期等字段的画廊自动执行一次单本详情补全。
- 新增“只获取详情元数据”的网络接口：只请求画廊主详情页并解析稳定/动态字段，不下载所有预览分页、图片、种子或归档信息。
- 单本补全最多并发 2 个，请求启动间隔至少 300 毫秒。
- 每本只自动回退一次；认证失败或限流时停止剩余详情请求。
- 详情补全成功后：
  - 分类、标题、作者、tags、页数和上传日期写入稳定快照；
  - 评分写入下载画廊动态快照；
  - 同步更新轻量索引和当前本地列表。
- 该行为与用户手动进入详情页联网刷新使用相同的字段解析和合并规则，避免两套逻辑再次产生差异。

### 合并与持久化

- 统一使用字段级快照合并。
- `.notLoaded` 字段不得覆盖本地已有值。
- `.loadedEmpty` 只有在接口明确返回空值时才能清除旧值。
- 修复 uploader、category、pageCount、thumbnail、postedAt、rating 等字段的旧值保护。
- 稳定元数据、动态评分和轻量索引按批次一次性保存，避免逐本多次提交 SwiftData。
- 更新成功后同步刷新内存中的下载摘要；即使已完成任务不再常驻协调器，本地详情也必须从持久化快照读取最新数据。

### 更新结果

最终提示显示：

- 目标总数；
- 完整成功数；
- 失败数；
- 仍不完整数；
- 执行过 gdata 重试的数量；
- 执行过详情补全的数量。

分类互斥：

- 成功：所需本地展示字段均已解析并持久化；
- 失败：重试及详情回退后仍没有可用响应；
- 不完整：至少成功写入部分字段，但仍有 `.notLoaded` 字段。

三类总数必须等于本次目标数。


